{
  "name": "dolla-if",
  "version": "1.0.0",
  "description": "A wrapper that allows if-else statements to be used as expressions, for vegetarians",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest"
  },
  "author": "Alex Szabo (delanni.alex@gmail.com)",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.9.0"
  },
  "publishConfig": { "registry": "https://npm.pkg.github.com/" }
}
